# Normalize line endings to LF in the repo (keeps Docker/Linux + CI happy,
# regardless of the OS that checked out the files).
* text=auto eol=lf

# Treat these as binary (never touch line endings).
*.png binary
*.jpg binary
*.pdf binary
